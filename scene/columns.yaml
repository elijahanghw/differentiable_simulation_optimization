# Columns — Tall boxes, procedural infinite world.
# Cell size is set automatically to cam_max_range at runtime.
# K = 9 × obstacles_per_cell boxes are loaded per step (3×3 cell neighborhood).
arena_z_min: -1.0
arena_z_max: 0.0

obstacles_per_cell: 2

box_hx_min: 0.05
box_hx_max: 0.20
box_hy_min: 0.05
box_hy_max: 0.20
box_hz_min: 3.00
box_hz_max: 5.00
